Fancy news tangles

Using:
 * Express
 * React, Flux, Jest
 * PureCSS
 * Webpack

Usage for development:
 * `$ npm install`
 * `$ npm run build`
 * `$ npm start`

Usage for development:
 * `$ npm install`
 * `$ npm run build-production`
 * `$ npm run production`

